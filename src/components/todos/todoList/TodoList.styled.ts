import { css } from "styled-components";

const inputWrapper = css`
  display: flex;
  gap: 6px;
`;

const buttonWrapper = css`
  display: flex;
  align-items: center;
  gap: 6px;
`;

export { inputWrapper, buttonWrapper };
