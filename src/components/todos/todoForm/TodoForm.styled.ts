import styled, { css } from "styled-components";

const Form = styled.form`
  width: 100%;
  display: flex;
  gap: 24px;
`;

const wrapper = css`
  width: 100%;
  display: flex;
`;

export { Form, wrapper };
